.DEFAULT_VIEW.center]; this.zoom=DEFAULT_VIEW.zoom; this.pitch=0; this.bearing=0; this.osm=true; this.tileCache=new Map(); this.layers={roads:true,hazards:true,satellite:true,buildings:true,shelters:true,observations:true,route:true}; this.drag=null; this.drawing=null; this.hit=[];
      new ResizeObserver(()=>this.resize()).observe(canvas.parentElement); this.bind(); this.resize();
    }
    worldSize(){return 512*2**this.zoom;} project(coord){const w=lngLatWorld(coord[0],coord[1]),c=lngLatWorld(this.center[0],this.center[1]); let dx=w.x-c.x;if(dx>.5)dx-=1;if(dx<-.5)dx+=1;const dy=w.y-c.y;const [rx,ry]=rotate(dx*this.worldSize(),dy*this.worldSize()*Math.max(.25,Math.cos(this.pitch*Math.PI/180)),-this.bearing*Math.PI/180);return[this.width/2+rx,this.height/2+ry];}
    unproject(x,y){let dx=x-this.width/2,dy=y-this.height/2;[dx,dy]=rotate(dx,dy,this.bearing*Math.PI/180);dy/=Math.max(.25,Math.cos(this.pitch*Math.PI/180));const c=lngLatWorld(this.center[0],this.center[1]);return worldLngLat(c.x+dx/this.worldSize(),Math.max(.001,Math.min(.999,c.y+dy/this.worldSize())));}
    resize(){const r=this.canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);this.canvas.width=Math.max(1,Math.round(r.width*d));this.canvas.height=Math.max(1,Math.round(r.height*d));this.ctx.setTransform(d,0,0,d,0,0);this.width=r.width;this.height=r.height;this.render();}
    bind(){this.canvas.addEventListener('contextmenu',e=>e.preventDefault());this.canvas.addEventListener('pointerdown',e=>{if(e.button!==0&&e.button!==2)return;if(e.button===0&&(interactionMode||this.drawing)){e.preventDefault();handleMapAction(this.unproject(e.offsetX,e.offsetY));return;}this.canvas.setPointerCapture(e.pointerId);this.drag={id:e.pointerId,mode:e.button===2||e.shiftKey?'rotate':'pan',x:e.clientX,y:e.clientY,center:lngLatWorld(...this.center),bearing:this.bearing,pitch:this.pitch};});this.canvas.addEventListener('pointermove',e=>{if(!this.drag||e.pointerId!==this.drag.id)return;const dx=e.clientX-this.drag.x,dy=e.clientY-this.drag.y;if(this.drag.mode==='rotate'){this.bearing=normalizeBearing(this.drag.bearing+dx*.35);this.pitch=Math.max(0,Math.min(65,this.drag.pitch-dy*.28));}else{const [ux,uy]=rotate(dx,dy,this.bearing*Math.PI/180);this.center=worldLngLat(this.drag.center.x-ux/this.worldSize(),this.drag.center.y-uy/(this.worldSize()*Math.max(.25,Math.cos(this.pitch*Math.PI/180))));}this.render();});const end=e=>{if(this.drag&&e.pointerId===this.drag.id){this.drag=null;persistView();}};this.canvas.addEventListener('pointerup',end);this.canvas.addEventListener('pointercancel',end);this.canvas.addEventListener('wheel',e=>{e.preventDefault();this.zoom=Math.max(2,Math.min(20,this.zoom+(e.deltaY<0?.55:-.55)));this.render();persistView();},{passive:false});}
    fit(bounds){if(!bounds)return;const[w,s,e,n]=bounds,c1=lngLatWorld(w,n),c2=lngLatWorld(e,s),dx=Math.max(1e-9,Math.abs(c2.x-c1.x)),dy=Math.max(1e-9,Math.abs(c2.y-c1.y));this.center=[(w+e)/2,(s+n)/2];this.zoom=Math.max(2,Math.min(19,Math.min(Math.log2((this.width-100)/(512*dx)),Math.log2((this.height-100)/(512*dy)))));this.render();}
    fly(coord,zoom=null){this.center=[...coord];if(zoom!==null)this.zoom=zoom;this.render();persistView();}
    getTile(z,x,y){const n=2**z,xx=((x%n)+n)%n,key=`${z}/${xx}/${y}`;let r=this.tileCache.get(key);if(r)return r;const img=new Image();img.decoding='async';img.crossOrigin='anonymous';r={img,state:'loading'};img.onload=()=>{r.state='ready';this.render();};img.onerror=()=>r.state='error';img.src=`https://tile.openstreetmap.org/${z}/${xx}/${y}.png`;this.tileCache.set(key,r);return r;}
    drawOsm(){if(!this.osm||!navigator.onLine)return;const z=Math.max(1,Math.min(19,Math.floor(this.zoom))),n=2**z,c=lngLatWorld(...this.center),rad=Math.hypot(this.width,this.height)/this.worldSize()/Math.max(.25,Math.cos(this.pitch*Math.PI/180));let count=0;for(let y=Math.max(0,Math.floor((c.y-rad)*n)-1);y<=Math.min(n-1,Math.floor((c.y+rad)*n)+1)&&count<180;y++)for(let x=Math.floor((c.x-rad)*n)-1;x<=Math.floor((c.x+rad)*n)+1&&count<180;x++){const r=this.getTile(z,x,y);if(r.state!=='ready')continue;const tl=this.project(worldLngLat(x/n,y/n)),tr=this.project(worldLngLat((x+1)/n,y/n)),bl=this.project(worldLngLat(x/n,(y+1)/n));this.ctx.save();this.ctx.transform((tr[0]-tl[0])/256,(tr[1]-tl[1])/256,(bl[0]-tl[0])/256,(bl[1]-tl[1])/256,tl[0],tl[1]);this.ctx.drawImage(r.img,0,0,256,256);this.ctx.restore();count++;}}
    line(coords,color,width=3,dash=[]){if(coords.length<2)return;this.ctx.beginPath();coords.forEach((c,i)=>{const p=this.project(c);i?this.ctx.lineTo(...p):this.ctx.moveTo(...p);});this.ctx.strokeStyle=color;this.ctx.lineWidth=width;this.ctx.lineCap='round';this.ctx.lineJoin='round';this.ctx.setLineDash(dash);this.ctx.stroke();this.ctx.setLineDash([]);}
    polygon(ring,fill,stroke,width=1){if(!ring?.length)return;this.ctx.beginPath();ring.forEach((c,i)=>{const p=this.project(c);i?this.ctx.lineTo(...p):this.ctx.moveTo(...p);});this.ctx.closePath();this.ctx.fillStyle=fill;this.ctx.fill();this.ctx.strokeStyle=stroke;this.ctx.lineWidth=width;this.ctx.stroke();}
    drawFeature(f,style={}){const g=f.geometry;if(!g)return;if(g.type==='Point'){const p=this.project(g.coordinates);this.ctx.beginPath();this.ctx.arc(p[0],p[1],style.radius||7,0,Math.PI*2);this.ctx.fillStyle=style.fill||'#276fb3';this.ctx.fill();this.ctx.strokeStyle='#fff';this.ctx.lineWidth=2;this.ctx.stroke();this.hit.push({feature:f,point:p});}else if(g.type==='LineString')this.line(g.coordinates,style.stroke||'#d13c32',style.width||4,style.dash||[]);else if(g.type==='Polygon')this.polygon(g.coordinates[0],style.fill||'rgba(211,60,50,.3)',style.stroke||'#b52f28',style.width||2);else if(g.type==='MultiPolygon')g.coordinates.forEach(p=>this.polygon(p[0],style.fill||'rgba(211,60,50,.3)',style.stroke||'#b52f28',style.width||2));}
    render(){if(!this.width)return;const c=this.ctx;c.clearRect(0,0,this.width,this.height);c.fillStyle='#dce4e8';c.fillRect(0,0,this.width,this.height);this.drawOsm();this.hit=[];if(this.layers.hazards)activeData.hazards.features.forEach(f=>this.drawFeature(f,{fill:'rgba(194,54,43,.26)',stroke:'#b62f28'}));if(this.layers.satellite)updates.filter(u=>u.featureCollection).flatMap(u=>u.featureCollection.features).forEach(f=>this.drawFeature(f,{fill:'rgba(151,61,176,.27)',stroke:'#7c3893'}));if(this.layers.roads&&roadRecord)roadRecord.ways.forEach(w=>{const coords=w.nodes.map(id=>roadRecord.nodes[id]).filter(Boolean);this.line(coords,'rgba(255,255,255,.92)',4);this.line(