e.data?.ok?resolve():reject(new Error(e.data?.error||'キャッシュ失敗'));};worker.postMessage({type:'PRECACHE_ALL'},[ch.port2]);});checks.push(['ok','アプリ本体']);checks.push([activePack?'ok':'warn',activePack?'地域パック':'地域パック未選択']);checks.push([roadRecord?'ok':'warn',roadRecord?'オフライン道路網':'道路網未保存']);checks.push(['ok',`現地報告 ${observations.length}件`]);dom.offlineCheckResults.replaceChildren();checks.forEach(([s,t])=>{const d=document.createElement('div');d.className=`check-result ${s}`;d.textContent=`${s==='ok'?'✓':'!'} ${t}`;dom.offlineCheckResults.append(d);});toast('オフライン準備を確認しました。','success');}finally{hideProgress();}}

  function bindEvents(){window.addEventListener('online',updateNetwork);window.addEventListener('offline',updateNetwork);dom.sidebarToggle.addEventListener('click',()=>{const c=dom.sidebar.classList.toggle('collapsed');dom.workspace.classList.toggle('sidebar-collapsed',c);dom.sidebarToggle.setAttribute('aria-expanded',String(!c));setTimeout(()=>map.resize(),30);});document.querySelectorAll('.mode-button').forEach(b=>b.addEventListener('click',()=>{appMode=b.dataset.mode;document.querySelectorAll('.mode-button').forEach(x=>x.classList.toggle('active',x===b));updateDecision();}));dom.installRegionButton.addEventListener('click',()=>{const e=regionCatalog.find(x=>x.id===dom.catalogSelect.value);if(!e)return handleError(new Error('地域を選択してください。'));installPackUrl(e.url,e.name).catch(handleError);});dom.importPackButton.addEventListener('click',()=>dom.packFileInput.click());dom.packFileInput.addEventListener('change',async()=>{const f=dom.packFileInput.files?.[0];dom.packFileInput.value='';if(f)installPack(await f.arrayBuffer(),f.name).catch(handleError);});dom.packSelect.addEventListener('change',()=>activatePack(dom.packSelect.value,true).catch(handleError));dom.downloadRoadsButton.addEventListener('click',()=>downloadRoads().catch(handleError));dom.deleteRoadsButton.addEventListener('click',async()=>{if(!activePack||!confirm('保存済み道路網を削除しますか？'))return;await dbDelete('roads',activePack.id);roadRecord=null;roadGraph=null;selectedRoute=null;updateDecision();map.render();});dom.updateCatalogSelect.addEventListener('change',showSelectedUpdateInfo);dom.downloadUpdateButton.addEventListener('click',()=>downloadUpdate().catch(handleError));dom.importUpdateButton.addEventListener('click',()=>dom.updateFileInput.click());dom.updateFileInput.addEventListener('change',async()=>{const f=dom.updateFileInput.files?.[0];dom.updateFileInput.value='';if(f)importUpdate(f).catch(handleError);});dom.openJaxaButton.addEventListener('click',()=>window.open(updateCatalog.event?.sourceUrl||JAXA_EVENT_URL,'_blank','noopener,noreferrer'));dom.deleteUpdatesButton.addEventListener('click',async()=>{if(!confirm('保存済み観測更新を削除しますか？'))return;for(const u of updates)await dbDelete('updates',u.id);updates=[];updateSourceStatus();updateDecision();map.render();});dom.locateButton.addEventListener('click',()=>navigator.geolocation?.getCurrentPosition(p=>{startPoint=[p.coords.longitude,p.coords.latitude];map.fly(startPoint,16);updateDecision();},e=>handleError(e),{enableHighAccuracy:true,timeout:20000}));dom.setStartButton.addEventListener('click',()=>{interactionMode='start';dom.drawingStatus.textContent='地図上で出発地点をクリックしてください。';});dom.setDestinationButton.addEventListener('click',()=>{interactionMode='destination';dom.drawingStatus.textContent='地図上で救助目的地をクリックしてください。';});dom.calculateEvacuationButton.addEventListener('click',()=>{try{calculateEvacuation();}catch(e){handleError(e);}});dom.calculateRescueRouteButton.addEventListener('click',()=>{try{calculateRescue();}catch(e){handleError(e);}});document.querySelectorAll('.draw-button').forEach(b=>b.addEventListener('click',()=>beginDraw(b.dataset.draw)));dom.finishDrawingButton.addEventListener('click',()=>finishDraw().catch(handleError));dom.cancelDrawingButton.addEventListener('click',cancelDraw);dom.exportSituationButton.addEventListener('click',exportSituation);dom.importSituationButton.addEventListener('click',()=>dom.situationFileInput.click());dom.situationFileInput.addEventListener('change',async()=>{const f=dom.situationFileInput.files?.[0];dom.situationFileInput.value='';if(f)importSituation(f).catch(handleError);});dom.osmBasemap.addEventListener('change',()=>{map.osm=dom.osmBasemap.checked;map.render();});document.querySelectorAll('[data-layer]').forEach(x=>x.addEventListener('change',()=>{map.layers[x.dataset.layer]=x.checked;map.render();}));dom.zoomInButton.addEventListener('click',()=>{map.zoom=Math.min(20,map.zoom+.7);map.render();});dom.zoomOutButton.addEventListener('click',()=>{map.zoom=Math.max(2,map.zoom-.7);map.render();});dom.toggle3dButton.addEventListener('click',()=>{map.pitch=map.pitch>5?0:52;map.render();});dom.bearingResetButton.addEventListener('click',()=>{map.bearing=0;map.render();});dom.homeViewButton.addEventListener('click',()=>activePack?.manifest?.bounds?map.fit(activePack.manifest.bounds):(map.center=[...DEFAULT_VIEW.center],map.zoom=DEFAULT_VIEW.zoom,map.render()));dom.requestPersistButton.addEventListener('click',async()=>{const ok=await navigator.storage?.persist?.();toast(ok?'永続保存が許可されました。':'永続保存は許可されませんでした。',ok?'success':'error');refreshStorage();});dom.prepareOfflineButton.addEventListener('click',()=>prepareOffline().catch(handleError));dom.clearCacheButton.addEventListener('click',async()=>{if(!confirm('アプリキャッシュを消去しますか？'))return;const n=await caches.keys();await Promise.all(n.filter(x=>x.startsWith(CACHE_PREFIX)).map(x=>caches.delete(x)));toast('キャッシュを消去しました。','success');});dom.deleteObservationsButton.addEventListener('click',async()=>{if(!confirm('現地障害・報告を全削除しますか？'))return;await dbClear(['observations']);observations=[];selectedRoute=null;renderObservationList();map.render();updateDecision();});dom.deleteAllPacksButton.addEventListener('click',async()=>{if(!confirm('地域パックと道路網を全削除しますか？'))return;await dbClear(['packs','files','roads']);await refreshPackSelect();await activatePack('');});dom.fullResetButton.addEventListener('click',async()=>{if(prompt('完全初期化する場合は RESET と入力してください。')!=='RESET')return;db.close();indexedDB.deleteDatabase(DB_NAME);const n=await caches.keys();await Promise.all(n.filter(x=>x.startsWith(CACHE_PREFIX)).map(x=>caches.delete(x)));location.reload();});}

  async function init(){dom.versionBadge.textContent=`ver ${APP_VERSION}`;bindEvents();updateNetwork();await registerSW().catch(console.warn);db=await openDatabase();await Promise.all([loadRegionCatalog(),loadUpdateCatalog()]);await refreshPackSelect();const id=dom.packSelect.value||await setting('activePackId','');await activatePack(id);await refreshStorage();showSelectedUpdateInfo();}
  init().catch(handleError);
})();
