lter(f=>shelterCompatible(f,type))){const n=nearestNode(sh.geometry.coordinates);if(!n)continue;const cost=result.dist.get(n.id);if(!Number.isFinite(cost))continue;candidates.push({shelter:sh,node:n.id,cost:cost+n.dist,coords:reconstruct(result.prev,n.id)});}candidates.sort((a,b)=>a.cost-b.cost);renderRoutes(candidates.slice(0,3));if(candidates[0])selectRoute(candidates[0]);else throw new Error('現在の障害情報を避けて到達できる避難候補がありません。');}
  function calculateRescue(){if(!roadRecord||!startPoint||!destinationPoint)throw new Error('道路網、出発点、救助目的地を準備してください。');const adj=buildGraph(dom.travelMode.value),s=nearestNode(startPoint),d=nearestNode(destinationPoint);if(!s||!d)throw new Error('道路網へ接続できません。');const r=dijkstra(s.id,adj),cost=r.dist.get(d.id);if(!Number.isFinite(cost))throw new Error('現在の障害情報を避ける進入ルートがありません。');const item={shelter:{properties:{name:'救助目的地'}},cost,coords:reconstruct(r.prev,d.id)};renderRoutes([item]);selectRoute(item);}
  function renderRoutes(items){dom.routeResults.replaceChildren();items.forEach((x,i)=>{const d=document.createElement('div');d.className='route-card';const name=x.shelter.properties?.name||'候補',km=(x.cost/1000).toFixed(2);d.innerHTML=`<strong>${i+1}. ${name}</strong><span>危険回避コスト換算 ${km} km / ${Math.max(1,Math.round(x.cost/(dom.travelMode.value==='vehicle'?500:80)))}分目安</span>`;const b=document.createElement('button');b.type='button';b.textContent='このルートを表示';b.addEventListener('click',()=>selectRoute(x));d.append(b);dom.routeResults.append(d);});}
  function selectRoute(item){selectedRoute={coordinates:item.coords,cost:item.cost,name:item.shelter.properties?.name||'目的地'};map.render();dom.mapDecisionBanner.textContent=`推奨: ${selectedRoute.name}。現地障害を避けたルートを表示中。`;dom.mapDecisionBanner.className='map-decision-banner safe';updateDecision();}

  function beginDraw(type){map.drawing={type,coords:[]};interactionMode=null;document.querySelectorAll('.draw-button').forEach(b=>b.classList.toggle('active',b.dataset.draw===type));dom.finishDrawingButton.disabled=true;dom.cancelDrawingButton.disabled=false;dom.drawingStatus.textContent=type==='Point'?'地図を1回クリックしてください。':type==='LineString'?'地図上で線の各点をクリックしてください。':'危険エリアの外周を3点以上クリックしてください。';map.render();}
  async function finishDraw(){if(!map.drawing)return;const d=map.drawing,c=d.coords;if((d.type==='Point'&&c.length<1)||(d.type==='LineString'&&c.length<2)||(d.type==='Polygon'&&c.length<3))throw new Error('入力点が不足しています。');const geometry=d.type==='Point'?{type:'Point',coordinates:c[0]}:d.type==='Polygon'?{type:'Polygon',coordinates:[[...c,c[0]]]}:{type:'LineString',coordinates:c};const now=new Date().toISOString(),f={type:'Feature',id:crypto.randomUUID(),geometry,properties:{packId:activePack?.id||'global',type:dom.obstacleType.value,severity:Number(dom.obstacleSeverity.value),note:dom.obstacleNote.value.trim(),source:'field_report',createdAt:now,updatedAt:now,status:'active'}};await dbPut('observations',{...f,id:f.id,packId:f.properties.packId});observations.unshift(f);cancelDraw();selectedRoute=null;renderObservationList();updateDecision();map.render();toast('現地障害・危険情報を保存し、経路判定へ反映しました。','success');}
  function cancelDraw(){map.drawing=null;document.querySelectorAll('.draw-button').forEach(b=>b.classList.remove('active'));dom.finishDrawingButton.disabled=true;dom.cancelDrawingButton.disabled=true;dom.drawingStatus.textContent='地点・線・エリアを選び、地図上をクリックしてください。';map.render();}
  function renderObservationList(){dom.observationList.replaceChildren();if(!observations.length){dom.observationList.textContent='この端末の現地報告はありません。';return;}observations.slice(0,30).forEach(o=>{const d=document.createElement('div');d.className='report-item';const p=o.properties||{};const title=document.createElement('strong');title.textContent=`${obstacleLabel(p.type)} `;const badge=document.createElement('span');badge.className=`observation-badge s${p.severity||3}`;badge.textContent=`重要度 ${p.severity||3}`;title.append(badge);const note=document.createElement('div');note.textContent=p.note||'メモなし';const meta=document.createElement('div');meta.textContent=formatDate(p.updatedAt);const del=document.createElement('button');del.type='button';del.className='danger-outline';del.textContent='削除';del.addEventListener('click',async()=>{if(!confirm('この報告を削除しますか？'))return;await dbDelete('observations',o.id);observations=observations.filter(x=>x.id!==o.id);selectedRoute=null;renderObservationList();map.render();updateDecision();});d.append(title,note,meta,del);dom.observationList.append(d);});}
  function obstacleLabel(t){return({road_block:'道路閉塞',collapse:'建物崩落',flood:'浸水',landslide:'土砂崩れ',fire:'火災',bridge:'橋梁異常',rescue_needed:'要救助',safe:'安全確認',no_entry:'立入禁止'})[t]||t;}
  function exportSituation(){const f=fc(observations.map(o=>({type:'Feature',id:o.id,geometry:o.geometry,properties:o.properties})));const blob=new Blob([JSON.stringify(f,null,2)],{type:'application/geo+json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`saikan-situation-${activePack?.id||'global'}-${Date.now()}.geojson`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
  async function importSituation(file){const j=JSON.parse(await file.text());validateFC(j,file.name);for(const f of j.features){const id=String(f.id||crypto.randomUUID()),p={...(f.properties||{}),packId:activePack?.id||f.properties?.packId||'global',updatedAt:f.properties?.updatedAt||new Date().toISOString()};await dbPut('observations',{...f,id,packId:p.packId,properties:p});}observations=await dbGetByIndex('observations','packId',activePack?.id||'global');renderObservationList();map.render();updateDecision();toast('状況データを統合しました。','success');}

  async function downloadUpdate(){const p=updateCatalog.products?.find(x=>x.id===dom.updateCatalogSelect.value);if(!p)throw new Error('観測更新を選択してください。');const rec={id:p.id,packId:activePack?.id||'global',...p,downloadedAt:new Date().toISOString()};if(p.dataUrl){const r=await fetch(p.dataUrl,{cache:'no-store'});if(!r.ok)throw new Error(`観測更新 HTTP ${r.status}`);const j=await r.json();validateFC(j,p.name);rec.featureCollection=j;}if(p.previewUrl){try{const r=await fetch(p.previewUrl,{mode:'cors',cache:'no-store'});if(r.ok)rec.previewBlob=await r.blob();}catch{rec.previewOffline=false;}}await dbPut('updates',rec);updates=await dbGetByIndex('updates','packId',rec.packId);updateSourceStatus();updateDecision();map.render();showSelectedUpdateInfo();toast(`${p.name}を観測資料として保存しました。`,'success');}
  async function importUpdate(file){const j=JSON.parse(await file.text());if(j.type==='FeatureCollection'){validateFC(j,file.name);const rec={id:`local-update-${Date.now()}`,packId:activePack?.id||'global',name:file.name,source:'local_import',usage:'routing_hazard',observedAt:new Date().toISOString(),downloadedAt:new Date().toISOString(),featureCollection:j};await dbPut('updates',rec);}else throw new Error('GeoJSON FeatureCollectionを指定してください。');updates=await dbGetByIndex('updates','packId',activePack?.id||'global');updateSourceStatus();updateDecision();map.render();toast('観測・被害更新を経路判定へ反映しました。','success');}
  function updateSourceStatus(){const alos=updates.filter(x=>String(x.satellite||'').includes('ALOS'));dom.alosStatus.textContent=alos.length?`${alos.length}件 / 最新 ${formatDate(alos.map(x=>x.observedAt).sort().at(-1))}`:'未取得';dom.qzssStatus.textContent=updates.some(x=>x.source==='QZSS')?'受信済み':'受信データなし';dom.jmaStatus.textContent=updates.some(x=>x.source==='JMA'||x.source==='Himawari')?'更新済み':'更新データなし';}

  function updateDecision(){const severe=observations.filter(o=>Number(o.properties?.severity||0)>=4).length,blocking=observations.filter(o=>['road_block','collapse','landslide','fire','bridge','no_entry'].includes(o.properties?.type)).length;let text,cls='';if(!activePack){text='地域パックを選択してください。オンライン中は全国地図を確認できます。';}else if(!roadRecord){text='地域データはあります。次に「OSM道路網を保存」すると、通信断後も避難・救助ルートを計算できます。';cls='warning';}else if(!startPoint){text=`道路網 ${roadRecord.ways.length}本を保存済みです。現在地または出発点を指定してください。`;cls='warning';}else if(severe){text=`重大な現地障害 ${severe}件を確認。記録済み障害を避けてルートを再計算してください。`;cls='danger';}else if(selectedRoute){text=`${selectedRoute.name}への障害回避ルートを表示中。現地状況に変化があればエリア記録して再計算してください。`;cls='safe';}else{text=`出発点を設定済み。現地障害 ${blocking}件を考慮して避難候補または救助進入ルートを計算できます。`;cls=blocking?'warning':'safe';}dom.decisionSummary.textContent=text;dom.mapDecisionBanner.textContent=text;dom.mapDecisionBanner.className=`map-decision-banner ${cls}`;const latestUpdate=updates.map(x=>x.observedAt||x.downloadedAt).filter(Boolean).sort().at(-1);dom.freshnessSummary.textContent=`基礎データ: ${formatDate(activePack?.manifest?.dataUpdatedAt)} / 道路網: ${formatDate(roadRecord?.downloadedAt)} / 衛星・防災更新: ${formatDate(latestUpdate)} / 現地報告: ${observations.length}件`;dom.emptyMapMessage.hidden=true;}
  function updateNetwork(){const online=navigator.onLine;dom.networkBadge.textContent=online?'オンライン':'オフライン';dom.networkBadge.className=`badge ${online?'badge-online':'badge-offline'}`;dom.staleBanner.hidden=online;if(!online)dom.staleText.textContent='保存済み地域・道路・観測更新・現地報告だけを使用しています。';map.render();}
  async function refreshStorage(){const e=await navigator.storage?.estimate?.(),p=await navigator.storage?.persisted?.();dom.storageSummary.textContent=`使用量 ${e?formatBytes(e.usage):'不明'} / 上限 ${e?formatBytes(e.quota):'不明'} | 地域 ${(await dbGetAll('packs')).length} | 道路 ${(await dbGetAll('roads')).length} | 更新 ${(await dbGetAll('updates')).length} | 現地報告 ${(await dbGetAll('observations')).length} | 永続保存 ${p?'許可済み':'未許可'}`;}
  async function registerSW(){if('serviceWorker'in navigator)return navigator.serviceWorker.register('./sw.js',{scope:'./'});return null;}
  async function prepareOffline(){const checks=[];showProgress('オフライン検証','アプリ本体をキャッシュしています…');try{const reg=await registerSW();await navigator.serviceWorker.ready;const worker=reg?.active||navigator.serviceWorker.controller;if(worker)await new Promise((resolve,reject)=>{const ch=new MessageChannel(),t=setTimeout(()=>reject(new Error('Service Worker応答なし')),30000);ch.port1.onmessage=e=>{clearTimeout(t);