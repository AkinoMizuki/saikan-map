rtPoint)this.drawFeature({geometry:{type:'Point',coordinates:startPoint}},{fill:'#18a15f',radius:9});if(destinationPoint)this.drawFeature({geometry:{type:'Point',coordinates:destinationPoint}},{fill:'#8d36b0',radius:9});if(this.drawing?.coords.length){const g=this.drawing.type==='Point'?{type:'Point',coordinates:this.drawing.coords[0]}:{type:this.drawing.type,coordinates:this.drawing.type==='Polygon'?[this.drawing.coords]:this.drawing.coords};this.drawFeature({geometry:g},{fill:'rgba(255,156,30,.3)',stroke:'#ff8a00',width:4,radius:9});}dom.bearingBadge.textContent=`方位 ${Math.round(this.bearing)}°`;dom.toggle3dButton.textContent=this.pitch>5?'2D':'3D';const mpp=40075016.686*Math.cos(this.center[1]*Math.PI/180)/this.worldSize();dom.mapScale.textContent=`約 ${mpp<1?mpp.toFixed(1):Math.round(mpp)} m/px / Zoom ${this.zoom.toFixed(1)}`;}
  }

  const map = new MapRenderer(dom.mapCanvas);
  async function persistView(){if(activePack)await setSetting(`view:${activePack.id}`,{center:map.center,zoom:map.zoom,pitch:map.pitch,bearing:map.bearing}).catch(()=>{});}
  function handleMapAction(coord){if(interactionMode==='start'){startPoint=coord;interactionMode=null;dom.drawingStatus.textContent='出発点を設定しました。';map.render();updateDecision();return;}if(interactionMode==='destination'){destinationPoint=coord;interactionMode=null;dom.drawingStatus.textContent='救助目的地を設定しました。';map.render();updateDecision();return;}if(map.drawing){map.drawing.coords.push(coord);dom.finishDrawingButton.disabled=map.drawing.type==='Point'?false:map.drawing.type==='LineString'?map.drawing.coords.length<2:map.drawing.coords.length<3;dom.drawingStatus.textContent=`${map.drawing.coords.length}点入力済み。${map.drawing.type==='Point'?'確定してください。':'続けてクリックするか確定してください。'}`;map.render();}}

  async function loadRegionCatalog(){try{const r=await fetch('./data/catalog.json',{cache:'no-store'});if(!r.ok)throw new Error();regionCatalog=(await r.json()).packs||[];}catch{regionCatalog=[];}dom.catalogSelect.replaceChildren();if(!regionCatalog.length){const o=document.createElement('option');o.textContent='地域カタログ取得失敗';o.value='';dom.catalogSelect.append(o);return;}regionCatalog.forEach(x=>{const o=document.createElement('option');o.value=x.id;o.textContent=x.name;dom.catalogSelect.append(o);});}
  async function loadUpdateCatalog(){try{const r=await fetch('./data/updates/catalog.json',{cache:'no-store'});if(!r.ok)throw new Error();updateCatalog=await r.json();}catch{updateCatalog={products:[],event:null};}dom.updateCatalogSelect.replaceChildren();if(!updateCatalog.products?.length){const o=document.createElement('option');o.value='';o.textContent='配布中の観測更新なし';dom.updateCatalogSelect.append(o);}else updateCatalog.products.forEach(x=>{const o=document.createElement('option');o.value=x.id;o.textContent=`${x.satellite||x.source}: ${x.name}`;dom.updateCatalogSelect.append(o);});showSelectedUpdateInfo();}
  function showSelectedUpdateInfo(){const p=updateCatalog.products?.find(x=>x.id===dom.updateCatalogSelect.value)||updateCatalog.products?.[0];dom.updateInfo.replaceChildren();if(!p){dom.updateInfo.textContent='配布中の更新はありません。';return;}const lines=[p.name,`観測: ${formatDate(p.observedAt)}`,p.beforeAt?`比較元: ${formatDate(p.beforeAt)}`:'',`解析: ${p.analysisType||'不明'} / ${p.method||''}`,p.warning].filter(Boolean);lines.forEach(t=>{const d=document.createElement('div');d.textContent=t;dom.updateInfo.append(d);});if(p.previewUrl){const img=document.createElement('img');img.className='preview-image';img.alt=`${p.name} ブラウズ画像`;img.loading='lazy';img.src=p.previewUrl;dom.updateInfo.append(img);} }
  async function refreshPackSelect(preferred=''){const packs=(await dbGetAll('packs')).sort((a,b)=>a.name.localeCompare(b.name,'ja'));dom.packSelect.replaceChildren();const none=document.createElement('option');none.value='';none.textContent='未選択';dom.packSelect.append(none);packs.forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=`${p.name} (${formatBytes(p.totalBytes)})`;dom.packSelect.append(o);});const wanted=preferred||await setting('activePackId','');if(packs.some(p=>p.id===wanted))dom.packSelect.value=wanted;}
  async function activatePack(id,fit=false){if(!id){activePack=null;activeData=emptyDataset();roadRecord=null;observations=[];updates=[];roadGraph=null;dom.dataBadge.textContent='地域未選択';dom.packDetails.textContent='地域を選択してください。';map.center=[...DEFAULT_VIEW.center];map.zoom=DEFAULT_VIEW.zoom;map.render();updateDecision();return;}activePack=await dbGet('packs',id);if(!activePack)throw new Error('地域パックがありません。');activeData=await loadDataset(activePack);roadRecord=await dbGet('roads',id);observations=await dbGetByIndex('observations','packId',id);updates=await dbGetByIndex('updates','packId',id);roadGraph=null;dom.dataBadge.textContent=activePack.name;dom.packDetails.textContent=`${activePack.description||''} / 更新 ${formatDate(activePack.manifest.dataUpdatedAt)} / 道路網 ${roadRecord?formatDate(roadRecord.downloadedAt):'未取得'}`;const v=await setting(`view:${id}`,null);if(v&&!fit){map.center=v.center;map.zoom=v.zoom;map.pitch=v.pitch||0;map.bearing=v.bearing||0;}else map.fit(activePack.manifest.bounds);await setSetting('activePackId',id);renderObservationList();updateSourceStatus();updateDecision();map.render();}

  async function downloadRoads(){if(!activePack?.manifest?.bounds)throw new Error('対象地域を選択してください。');const[w,s,e,n]=activePack.manifest.bounds;const area=Math.abs((e-w)*(n-s));if(area>.2&&!confirm('範囲が大きいため取得に時間がかかります。続行しますか？'))return;const q=`[out:json][timeout:90];way["highway"](${s},${w},${n},${e});out body;>;out skel qt;`;showProgress('道路網取得','OpenStreetMap道路網を取得しています…');try{const r=await fetch(OVERPASS_URL,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:`data=${encodeURIComponent(q)}`});if(!r.ok)throw new Error(`Overpass API HTTP ${r.status}`);const j=await r.json(),nodes={},ways=[];for(const x of j.elements||[])if(x.type==='node')nodes[x.id]=[x.lon,x.lat];else if(x.type==='way'&&x.nodes?.length>1)ways.push({id:x.id,nodes:x.nodes,tags:x.tags||{}});roadRecord={packId:activePack.id,downloadedAt:new Date().toISOString(),source:'OpenStreetMap / Overpass API',nodes,ways};await dbPut('roads',roadRecord);roadGraph=null;toast(`道路 ${ways.length}本を保存しました。`,'success');updateDecision();map.render();await refreshStorage();}finally{hideProgress();}}
  function wayAllowed(tags,mode){const h=tags.highway||'';if(tags.access==='no'||tags.access==='private')return false;if(mode==='vehicle')return !['footway','path','pedestrian','steps','cycleway','bridleway'].includes(h);if(mode==='wheelchair')return !['steps','path','track','bridleway','motorway','trunk'].includes(h);return !['motorway','motorway_link'].includes(h);}
  function featureBlocksEdge(feature,a,b){const p=feature.properties||{},sev=Number(p.severity||p.risk||3),type=p.type||p.hazard_type||'';if(type==='safe')return {block:false,mult:.65};const g=feature.geometry;if(!g)return{block:false,mult:1};let hit=false;if(g.type==='Point')hit=pointSegmentMeters(g.coordinates,a,b)<(sev>=4?45:25);else if(g.type==='LineString'){for(let i=1;i<g.coordinates.length&&!hit;i++)hit=segmentsIntersect(a,b,g.coordinates[i-1],g.coordinates[i]);}else if(g.type==='Polygon'){const mid=[(a[0]+b[0])/2,(a[1]+b[1])/2];hit=pointInPolygon(mid,g.coordinates[0])||pointInPolygon(a,g.coordinates[0])||pointInPolygon(b,g.coordinates[0]);}else if(g.type==='MultiPolygon'){const mid=[(a[0]+b[0])/2,(a[1]+b[1])/2];hit=g.coordinates.some(x=>pointInPolygon(mid,x[0]));}if(!hit)return{block:false,mult:1};const hard=['road_block','collapse','landslide','fire','bridge','no_entry'].includes(type)||sev>=4;return{block:hard,mult:1+sev*2};}
  function edgeRisk(a,b){let mult=1;for(const f of [...activeData.hazards.features,...updates.filter(u=>u.featureCollection).flatMap(u=>u.featureCollection.features),...observations]){const r=featureBlocksEdge(f,a,b);if(r.block)return Infinity;mult*=r.mult;}return Math.min(mult,25);}
  function buildGraph(mode){if(!roadRecord)return null;const adj=new Map();const add=(a,b,w)=>{if(!adj.has(a))adj.set(a,[]);adj.get(a).push({to:b,w});};for(const way of roadRecord.ways){if(!wayAllowed(way.tags,mode))continue;for(let i=1;i<way.nodes.length;i++){const a=way.nodes[i-1],b=way.nodes[i],ca=roadRecord.nodes[a],cb=roadRecord.nodes[b];if(!ca||!cb)continue;const risk=edgeRisk(ca,cb);if(!Number.isFinite(risk))continue;const w=haversine(ca,cb)*risk;add(a,b,w);if(!(mode==='vehicle'&&(way.tags.oneway==='yes'||way.tags.junction==='roundabout')))add(b,a,w);}}return adj;}
  function nearestNode(coord,max=1500){if(!roadRecord)return null;let best=null,dist=Infinity;for(const[id,c]of Object.entries(roadRecord.nodes)){const d=haversine(coord,c);if(d<dist){dist=d;best=id;}}return dist<=max?{id:best,dist}:null;}
  function dijkstra(start,adj){const dist=new Map([[start,0]]),prev=new Map(),heap=[[0,start]];while(heap.length){heap.sort((a,b)=>a[0]-b[0]);const[d,u]=heap.shift();if(d!==dist.get(u))continue;for(const e of adj.get(u)||[]){const nd=d+e.w;if(nd<(dist.get(e.to)??Infinity)){dist.set(e.to,nd);prev.set(e.to,u);heap.push([nd,e.to]);}}}return{dist,prev};}
  function reconstruct(prev,end){const ids=[];let u=end;while(u){ids.push(u);u=prev.get(u);}ids.reverse();return ids.map(id=>roadRecord.nodes[id]).filter(Boolean);}
  function shelterCompatible(f,type){const h=f.properties?.hazards||[];if(type==='earthquake')return h.includes('地震');if(type==='flood')return h.includes('洪水');if(type==='landslide')return h.some(x=>String(x).includes('崖崩れ')||String(x).includes('土石'));return true;}
  function calculateEvacuation(){if(!activePack)throw new Error('地域を選択してください。');if(!roadRecord)throw new Error('先にOSM道路網を保存してください。');if(!startPoint)throw new Error('現在地または出発点を指定してください。');const mode=dom.travelMode.value,adj=buildGraph(mode),s=nearestNode(startPoint);if(!s)throw new Error('出発点付近に利用可能な道路がありません。');const result=dijkstra(s.id,adj),type=dom.disasterType.value,candidates=[];for(const sh of activeData.shelters.features.fi